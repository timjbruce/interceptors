temporalio>=1.30
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
cryptography>=42.0
pydantic>=2.0
# Lets FastAPI read `Form(...)` params: RFC 8693 §2.1 requires the token-exchange
# request be application/x-www-form-urlencoded, not JSON.
python-multipart>=0.0.9

# Dev/test only:
pytest>=8.0
pytest-asyncio>=0.23
